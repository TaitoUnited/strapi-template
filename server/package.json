{
  "name": "strapi-template",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "unit": "echo TODO unit?",
    "test": "echo TODO test?",
    "lint": "echo TODO lint?",
    "develop": "cd strapi && npm run develop",
    "start": "cd strapi && npm run start",
    "build": "cd strapi && RUNNING_BUILD=true npm run build",
    "strapi": "cd strapi && npm run strapi"
  }
}
